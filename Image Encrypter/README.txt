Python code that encypts an image by changing every pixels RGB values. The file 'brock.before.jpg' is the sample input for the program. 'brock.after.png' is the outputted image.

NOTE: Encrypted files must end in '.png' extension in order for the decryption to work.